---
phase: 02-auth-access-control
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src-tauri/src/commands/auth.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/App.tsx
  - src/hooks/useAuth.ts
  - src/lib/tauri.ts
autonomous: false

requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08

must_haves:
  truths:
    - "User can create a system admin account on first launch"
    - "User can log in with correct password (Argon2id hashed)"
    - "Invalid credentials show generic error without revealing which field is wrong"
    - "Session locks automatically after configured inactivity timeout"
    - "Locked session can be unlocked with password"
    - "MFA setup displays QR code and only enables after code verification"
    - "Login prompts for TOTP code when user has MFA enabled"
    - "RBAC prevents unauthorized actions (e.g., FrontDesk cannot create clinical records)"
    - "Break-glass provides time-limited elevated access with mandatory reason"
    - "Touch ID is available for session unlock on supported hardware"
  artifacts:
    - path: "src/App.tsx"
      provides: "Fully gated application with auth flow"
    - path: "src-tauri/src/commands/auth.rs"
      provides: "Login flow with MFA integration"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useAuth.ts"
      via: "Auth state drives conditional rendering"
      pattern: "useAuth"
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/lib/tauri.ts"
      via: "Login command invocation"
      pattern: "commands\\.login"
---

<objective>
Final integration wiring and human verification of the complete auth system. Ensure the login flow integrates MFA check, the first-run setup works, and all 8 AUTH requirements are met end-to-end.

Purpose: Plans 01-04 built the pieces independently. This plan ensures they work together as a coherent auth system and gets human confirmation that all requirements are met.

Output: Fully integrated authentication system verified against all 8 AUTH requirements.
</objective>

<execution_context>
@/Users/omarsharaf96/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omarsharaf96/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-access-control/02-RESEARCH.md
@.planning/phases/02-auth-access-control/02-01-SUMMARY.md
@.planning/phases/02-auth-access-control/02-02-SUMMARY.md
@.planning/phases/02-auth-access-control/02-03-SUMMARY.md
@.planning/phases/02-auth-access-control/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration wiring -- MFA in login flow, first-run detection, complete auth pipeline</name>
  <files>
    src-tauri/src/commands/auth.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/lib.rs,
    src/App.tsx,
    src/hooks/useAuth.ts,
    src/lib/tauri.ts
  </files>
  <action>
    1. Update the login command in src-tauri/src/commands/auth.rs to include MFA status in response:
       - After successful password verification, check if user.totp_enabled is true.
       - If MFA is enabled, return a response indicating MFA is required (e.g., LoginResponse with mfa_required: true, partial session). Do NOT create full session yet.
       - Add a `complete_login(db, session, user_id, totp_code)` command that: verifies TOTP code, creates the actual session, returns full LoginResponse. This separates password auth from MFA verification.
       - Add a `check_first_run(db)` command that returns bool based on SELECT COUNT(*) FROM users == 0.
       - Register complete_login and check_first_run in lib.rs generate_handler.
       - Add invoke wrappers for completeLogin and checkFirstRun in src/lib/tauri.ts.

    2. Ensure first-run detection works in App.tsx:
       - On mount, before showing login, call commands.checkFirstRun().
       - If first run (no users), show RegisterForm with role locked to system_admin.
       - After first admin is created, transition to LoginForm.

    3. Update useAuth.ts login flow to handle MFA:
       - If login response has mfa_required=true, set mfaRequired state and store pending user_id.
       - verifyMfa calls commands.completeLogin with user_id and TOTP code.
       - On success, complete the auth state transition to authenticated.

    4. Verify the complete login flow integrates correctly:
       - Login form -> password check -> if MFA enabled, show MfaPrompt -> verify TOTP -> create session -> show main app.
       - Login form -> password check -> if MFA not enabled -> create session directly -> show main app.
       - Session lock -> LockScreen -> password (or Touch ID) -> unlock -> resume.

    5. Ensure all Tauri commands from Plans 01-04 are registered in lib.rs generate_handler. Do a final check that nothing is missing.
  </action>
  <verify>
    <automated>cd /Users/omarsharaf96/Documents/GitHub/MedArc/src-tauri && cargo check 2>&1 | tail -10 && cd /Users/omarsharaf96/Documents/GitHub/MedArc && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Login flow handles both MFA and non-MFA users correctly
    - First-run detection shows registration form with system_admin role
    - complete_login command verifies TOTP before creating session
    - check_first_run command correctly detects empty users table
    - Both cargo check and tsc --noEmit pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 8 AUTH requirements</name>
  <files>N/A</files>
  <action>
    Present the complete authentication system for human verification. The system includes:
    - User registration and login with Argon2id password hashing (AUTH-01, AUTH-02)
    - Session management with configurable auto-lock (AUTH-03)
    - Touch ID biometric unlock (AUTH-04)
    - TOTP multi-factor authentication with QR code enrollment (AUTH-05)
    - 5-role RBAC with field-level access control (AUTH-06, AUTH-07)
    - Emergency break-glass access with logging (AUTH-08)

    Run the application with: cd /Users/omarsharaf96/Documents/GitHub/MedArc && npx tauri dev

    Verification steps:

    AUTH-01 User account creation:
    1. On first launch, you should see a registration form (not login)
    2. Create a system admin account (username, password 12+ chars, display name)
    3. Verify the account is created and you are logged in

    AUTH-02 Password authentication:
    4. Log out, then log back in with correct credentials -- should succeed
    5. Try logging in with wrong password -- should see generic "Invalid credentials" error
    6. Try a password shorter than 12 characters during registration -- should be rejected

    AUTH-03 Session auto-lock:
    7. After login, wait for the configured timeout (you may want to temporarily set timeout to 1 minute for testing)
    8. Verify the lock screen overlay appears
    9. Unlock with your password -- should return to main app

    AUTH-04 Touch ID:
    10. If on a Mac with Touch ID: check if Touch ID option appears on lock screen
    11. If no Touch ID hardware: verify it gracefully shows password-only unlock

    AUTH-05 TOTP MFA:
    12. Find MFA setup in the app
    13. Click to enable MFA -- should see a QR code
    14. Scan QR with an authenticator app (Google Authenticator, Authy, etc.)
    15. Enter the 6-digit code -- should enable MFA
    16. Log out and log back in -- should prompt for TOTP code after password
    17. Enter correct TOTP code -- should complete login

    AUTH-06 and AUTH-07 RBAC:
    18. As system admin, try creating a FHIR resource -- should succeed
    19. (If time permits) Create a second user with "front_desk" role, log in as them, try creating a FHIR resource -- should be denied

    AUTH-08 Break-glass:
    20. Verify break-glass activation requires a reason
    21. Check that break_glass_log table has entries after activation
  </action>
  <verify>Human approval of all AUTH requirements</verify>
  <done>
    - All 8 AUTH requirements verified working by human review
    - Type "approved" or describe any issues found
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/omarsharaf96/Documents/GitHub/MedArc/src-tauri && cargo test 2>&1 | tail -20
```
All Rust tests pass (password, session, RBAC, TOTP, migrations).

```bash
cd /Users/omarsharaf96/Documents/GitHub/MedArc && npx tsc --noEmit 2>&1 | tail -10
```
TypeScript compiles cleanly.

```bash
cd /Users/omarsharaf96/Documents/GitHub/MedArc/src-tauri && cargo check 2>&1 | tail -5
```
Full build compiles.
</verification>

<success_criteria>
- Human has verified the complete authentication flow works end-to-end
- All 8 AUTH requirements (AUTH-01 through AUTH-08) are confirmed working
- First-run creates system admin account
- Login/logout/lock/unlock cycle works correctly
- MFA enrollment and login verification work with a real authenticator app
- RBAC correctly restricts actions based on role
- Application is fully gated behind authentication
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-access-control/02-05-SUMMARY.md`
</output>
