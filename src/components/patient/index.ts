/**
 * Barrel export for patient components.
 * Import patient components from this file rather than the individual modules
 * to keep import paths stable if internal file names change.
 */
export { PatientListPage } from "./PatientListPage";
