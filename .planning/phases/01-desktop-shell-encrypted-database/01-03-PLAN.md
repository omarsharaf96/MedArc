---
phase: 01-desktop-shell-encrypted-database
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/App.tsx
  - src/components/DatabaseStatus.tsx
  - src/components/FhirExplorer.tsx
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04
  - FOUN-05
  - FOUN-06

must_haves:
  truths:
    - "User launches the Tauri app and sees the MedArc desktop window with database status"
    - "Database file on disk is encrypted and unreadable by plain sqlite3"
    - "Keychain contains the encryption key entry"
    - "User can create, view, and delete FHIR resources through the UI"
    - "App survives restart with data persisted and re-accessible"
  artifacts:
    - path: "src/components/DatabaseStatus.tsx"
      provides: "Status card showing encryption state, cipher version, resource count"
      min_lines: 20
    - path: "src/components/FhirExplorer.tsx"
      provides: "Interactive panel to create and list FHIR resources for verification"
      min_lines: 40
  key_links:
    - from: "src/components/DatabaseStatus.tsx"
      to: "src/lib/tauri.ts"
      via: "calls commands.checkDb() on mount"
      pattern: "commands\\.checkDb"
    - from: "src/components/FhirExplorer.tsx"
      to: "src/lib/tauri.ts"
      via: "calls CRUD commands for resource management"
      pattern: "commands\\.(createResource|listResources|deleteResource)"
---

<objective>
Polish the frontend UI into proper components and perform end-to-end verification of all Phase 1 requirements through a human-verified checkpoint.

Purpose: This plan ensures the Phase 1 foundation is solid before any subsequent phases build on it. It extracts UI into proper components and then verifies every requirement with a human walkthrough.

Output: Clean component-based React UI and verified confirmation that all six FOUN requirements are met.
</objective>

<execution_context>
@/Users/omarsharaf96/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omarsharaf96/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-desktop-shell-encrypted-database/01-RESEARCH.md
@.planning/phases/01-desktop-shell-encrypted-database/01-01-SUMMARY.md
@.planning/phases/01-desktop-shell-encrypted-database/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract frontend into proper components with database status and FHIR explorer</name>
  <files>
    src/App.tsx
    src/components/DatabaseStatus.tsx
    src/components/FhirExplorer.tsx
  </files>
  <action>
    Refactor the App.tsx from Plan 02 into clean, separated components.

    **DatabaseStatus component (src/components/DatabaseStatus.tsx):**
    - On mount, call `commands.checkDb()` and `commands.getAppInfo()`
    - Display a status card with:
      - Encryption status (green indicator + "Encrypted" text)
      - SQLCipher version
      - Database page count
      - App version
      - Database file path
    - Handle loading and error states
    - Use Tailwind: rounded card with shadow, green/red status indicator, monospace for version numbers

    **FhirExplorer component (src/components/FhirExplorer.tsx):**
    - Display a list of FHIR resources (call `commands.listResources()` on mount)
    - Show each resource as a card: resource type, ID (truncated), last updated timestamp
    - Include a "Create Test Patient" button that creates a sample Patient resource with realistic demo data:
      ```json
      {
        "resourceType": "Patient",
        "name": [{ "family": "Smith", "given": ["John"] }],
        "birthDate": "1985-03-15",
        "gender": "male",
        "telecom": [{ "system": "phone", "value": "555-0123" }]
      }
      ```
    - Include a "Delete" button on each resource card
    - After create or delete, refresh the list
    - Show total count in the section header
    - Handle empty state: "No resources yet. Create a test patient to verify the database."

    **Updated App.tsx:**
    - Import and render DatabaseStatus and FhirExplorer components
    - Layout: centered max-w-4xl container with:
      - "MedArc" heading with subtitle "Electronic Medical Records"
      - DatabaseStatus component at the top
      - FhirExplorer component below
    - Use Tailwind: clean white/gray color scheme, proper spacing, responsive layout

    Run `npm run tauri dev` to verify the UI renders correctly with both components.
  </action>
  <verify>
    Run `cd /Users/omarsharaf96/Documents/GitHub/MedArc && npm run tauri dev` -- app launches and shows DatabaseStatus card with encryption info and FhirExplorer with create/delete functionality.
  </verify>
  <done>
    Frontend is organized into proper React components. DatabaseStatus shows encryption verification. FhirExplorer provides interactive CRUD for testing. UI is clean and functional.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 1 FOUN requirements</name>
  <files>
    src/App.tsx
    src/components/DatabaseStatus.tsx
    src/components/FhirExplorer.tsx
  </files>
  <action>
    Present the running application for human verification of all six FOUN requirements. The app should already be running from Task 1 verification. Guide the user through each check.
  </action>
  <verify>
    Human confirms all 7 verification steps pass (see how-to-verify below).
  </verify>
  <done>
    All FOUN-01 through FOUN-06 requirements confirmed working by human review.
  </done>
  <what-built>
    Complete Phase 1 foundation: Tauri 2.x desktop app with SQLCipher-encrypted database, macOS Keychain key storage, Rust-native FHIR CRUD commands, and React frontend.
  </what-built>
  <how-to-verify>
    1. **FOUN-01 (Tauri app launches):** Run `npm run tauri dev` from the project root. Confirm:
       - A macOS desktop window opens titled "MedArc"
       - The React UI renders with the MedArc heading and database status

    2. **FOUN-02 (SQLCipher encryption):** In a terminal, run:
       ```bash
       sqlite3 ~/Library/Application\ Support/com.medarc.emr/medarc.db "SELECT * FROM app_metadata;"
       ```
       Confirm this FAILS with "file is encrypted or is not a database"

    3. **FOUN-03 (Keychain storage):** Open Keychain Access.app, search for "medarc". Confirm:
       - An entry exists for "com.medarc.emr" with account "database-encryption-key"
       - The entry is of type "application password"

    4. **FOUN-04 (FHIR resources):** In the app UI:
       - Click "Create Test Patient" -- a Patient resource card should appear
       - Click it again -- a second resource should appear with a different ID
       - Verify each card shows resource type "Patient", a UUID, and a timestamp

    5. **FOUN-05 (Migrations):** The fact that the app launched and shows database status proves migrations ran. The DatabaseStatus card should show a page count > 0.

    6. **FOUN-06 (Rust-native commands):** All the operations above executed through Rust Tauri commands. Confirm:
       - No Python process is running alongside the app
       - The DatabaseStatus shows cipher_version (proving Rust->SQLCipher path works)

    7. **Persistence test:** Close the app completely (Cmd+Q), then relaunch with `npm run tauri dev`. Confirm:
       - Previously created Patient resources still appear in the list
       - Database status still shows encrypted
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All six FOUN requirements verified by human walkthrough:
1. FOUN-01: App launches as macOS desktop app with React in WKWebView
2. FOUN-02: Database file is SQLCipher-encrypted (unreadable by sqlite3)
3. FOUN-03: Key in macOS Keychain (visible in Keychain Access)
4. FOUN-04: FHIR resources stored as JSON, creatable/listable through UI
5. FOUN-05: Migrations ran (database tables exist, status shows page count)
6. FOUN-06: All operations via Rust commands (no Python dependency)
</verification>

<success_criteria>
- All 7 verification steps pass human review
- App launches, creates resources, persists data, survives restart
- Encryption proven by sqlite3 failure on raw file
- Keychain entry confirmed in Keychain Access
- No Python dependency present
</success_criteria>

<output>
After completion, create `.planning/phases/01-desktop-shell-encrypted-database/01-03-SUMMARY.md`
</output>
